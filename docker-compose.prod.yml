services:
  backend:
    build:
      target: production
    ports:
      - "5000:5000"
    restart: always

  frontend:
    build:
      context: ../lifeflow-frontend/my-app
    ports:
      - "3000:80"
    restart: always
